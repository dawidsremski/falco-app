import { IntersectionObserverDirectiveDirective } from './intersection-observer-directive.directive';

describe('IntersectionObserverDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IntersectionObserverDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
